# Template for exposing several bare-metal pulsed nodes behind one hostname.
#
# Browser URLs look like:
# - https://pulsed.example.com/?pulsed_node=node-a
# - https://pulsed.example.com/?pulsed_node=node-b
#
# On each bare-metal node, advertise the shared host with that node's query key:
# PULSED_ADVERTISE_HTTP=https://pulsed.example.com/?pulsed_node=node-a
#
# Traefik routes by query parameter before forwarding the request, so hot-potato
# refreshes and peer links still land on the intended node without per-node DNS.
# Requests without a matching pulsed_node value fall back to pulsed-any. Keep that
# Endpoints list limited to nodes that should receive unrouted fallback traffic.
apiVersion: v1
kind: Service
metadata:
  name: pulsed-node-a
  namespace: default
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
---
apiVersion: v1
kind: Endpoints
metadata:
  name: pulsed-node-a
  namespace: default
subsets:
  - addresses:
      - ip: 10.0.1.25
    ports:
      - name: http
        port: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: pulsed-node-b
  namespace: default
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
---
apiVersion: v1
kind: Endpoints
metadata:
  name: pulsed-node-b
  namespace: default
subsets:
  - addresses:
      - ip: 10.0.1.26
    ports:
      - name: http
        port: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: pulsed-any
  namespace: default
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
---
apiVersion: v1
kind: Endpoints
metadata:
  name: pulsed-any
  namespace: default
subsets:
  - addresses:
      - ip: 10.0.1.25
      - ip: 10.0.1.26
    ports:
      - name: http
        port: 8080
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: pulsed
  namespace: default
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`pulsed.example.com`) && Query(`pulsed_node`, `node-a`)
      kind: Rule
      priority: 100
      services:
        - name: pulsed-node-a
          port: 8080
    - match: Host(`pulsed.example.com`) && Query(`pulsed_node`, `node-b`)
      kind: Rule
      priority: 100
      services:
        - name: pulsed-node-b
          port: 8080
    - match: Host(`pulsed.example.com`)
      kind: Rule
      priority: 1
      services:
        - name: pulsed-any
          port: 8080
  tls: {}
