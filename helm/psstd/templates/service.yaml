apiVersion: v1
kind: Service
metadata:
  name: {{ include "pulsed.fullname" . }}
spec:
  selector: {{ include "pulsed.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: 80
      targetPort: http
